/**
 * @param {number} courses
 * @param {Array<[number, number]>} prerequisites
 * @return {boolean}
 */
export default function courseDependency(
  courses: number,
  prerequisites: Array<[number, number]>,
): boolean {
  throw 'Not implemented!';
}
